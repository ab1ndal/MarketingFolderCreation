---
phase: 03-packaging-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - dist/MarketingFolderCreation/
autonomous: false
requirements:
  - PKG-01
  - PKG-02
  - PERF-01

must_haves:
  truths:
    - "dist/MarketingFolderCreation/ folder exists after build with MarketingFolderCreation.exe inside"
    - "The bundle folder contains a templates/ subdirectory with A250.docx"
    - "The bundle folder contains PyQt6 platform plugins (qwindows.dll or equivalent)"
    - "Launching MarketingFolderCreation.exe opens the PyQt6 window without a console terminal"
    - "A250 document generation works from the bundled exe (template resolved via _MEIPASS)"
    - "Application window appears within 3 seconds of exe invocation"
  artifacts:
    - path: "dist/MarketingFolderCreation/MarketingFolderCreation.exe"
      provides: "Standalone Windows executable"
    - path: "dist/MarketingFolderCreation/templates/A250.docx"
      provides: "Bundled A250 template accessible at runtime via sys._MEIPASS"
  key_links:
    - from: "dist/MarketingFolderCreation/MarketingFolderCreation.exe"
      to: "dist/MarketingFolderCreation/templates/A250.docx"
      via: "sys._MEIPASS at runtime"
      pattern: "_MEIPASS"
    - from: "dist/MarketingFolderCreation/MarketingFolderCreation.exe"
      to: "PyQt6/Qt6/plugins/platforms/qwindows.dll"
      via: "PyInstaller PyQt6 hook"
      pattern: "qwindows"
---

<objective>
Build the --onedir bundle using MarketingFolderCreation.spec and verify the exe runs correctly on this machine.

Purpose: Produce the distributable folder that can be placed on the network share for users to launch. Verify PyQt6 starts, the A250 template resolves, and startup is fast enough for network share usage.

Output:
- dist/MarketingFolderCreation/ — complete distributable bundle folder
- Human verification that the exe opens correctly and A250 generation works
</objective>

<execution_context>
@C:/Users/abindal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abindal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-packaging-deployment/03-01-SUMMARY.md
@MarketingFolderCreation.spec

<interfaces>
<!-- Key facts for the executor. -->

PyInstaller build command for a spec file:
  pyinstaller MarketingFolderCreation.spec --clean --noconfirm

Flags:
  --clean     clears PyInstaller's cache before building (prevents stale artifacts)
  --noconfirm overrides the "dist/MarketingFolderCreation already exists" prompt

Output location after build:
  dist/MarketingFolderCreation/            <- the distributable folder
  dist/MarketingFolderCreation/MarketingFolderCreation.exe
  dist/MarketingFolderCreation/templates/A250.docx    <- from datas
  dist/MarketingFolderCreation/PyQt6/Qt6/plugins/platforms/qwindows.dll  (or similar path)

build/ folder is PyInstaller's work directory — not needed for distribution.

Smoke test sequence for the built bundle (run from project root, not inside dist/):
  1. Check exe exists: dist/MarketingFolderCreation/MarketingFolderCreation.exe
  2. Check template bundled: dist/MarketingFolderCreation/templates/A250.docx
  3. Check platform plugin bundled: find dist/MarketingFolderCreation/ -name "qwindows.dll"
  4. Launch exe: start dist/MarketingFolderCreation/MarketingFolderCreation.exe
     (human verifies window appears within 3 seconds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the --onedir bundle with PyInstaller</name>
  <files>dist/MarketingFolderCreation/</files>

  <read_first>
    - MarketingFolderCreation.spec (confirm it has COLLECT block and correct datas — from 03-01)
    - .planning/phases/03-packaging-deployment/03-01-SUMMARY.md (confirm 03-01 completed successfully)
  </read_first>

  <action>
Run the PyInstaller build from the project root directory:

```bash
pyinstaller MarketingFolderCreation.spec --clean --noconfirm
```

Expected output: PyInstaller prints analysis, collection, and "Building EXE" then "Building COLLECT" steps. Final line should be "Successfully built MarketingFolderCreation.exe" or similar completion message.

If the build fails, diagnose from the PyInstaller output:
- "ModuleNotFoundError" in build log → add the missing module to hiddenimports in the spec
- "WARNING: Hidden import ... not found" → acceptable warning for win32com submodules, does not fail the build
- "error: icon file not found" → check FolderCreatorTool.ico exists in project root

Do NOT modify the spec during this task. If spec changes are needed, fix the spec (from 03-01 output) before rerunning.

After the build completes, run these verification checks:
```bash
# Exe exists
test -f dist/MarketingFolderCreation/MarketingFolderCreation.exe && echo "EXE: FOUND" || echo "EXE: MISSING"

# Template bundled
test -f "dist/MarketingFolderCreation/templates/A250.docx" && echo "TEMPLATE: FOUND" || echo "TEMPLATE: MISSING"

# Platform plugin bundled
find dist/MarketingFolderCreation/ -name "qwindows.dll" -o -name "qwindows*.dll" 2>/dev/null | head -3
```
  </action>

  <verify>
    <automated>
python -c "
import os, sys
from pathlib import Path

exe = Path('dist/MarketingFolderCreation/MarketingFolderCreation.exe')
template = Path('dist/MarketingFolderCreation/templates/A250.docx')

assert exe.exists(), f'EXE missing: {exe}'
assert template.exists(), f'Template missing: {template}'

# Check qwindows.dll somewhere under dist/
found_platform = list(Path('dist/MarketingFolderCreation').rglob('qwindows*.dll'))
assert found_platform, 'No qwindows*.dll found in bundle — PyQt6 will fail to start'

print('PASS: Bundle structure verified')
print(f'  EXE:      {exe}')
print(f'  Template: {template}')
print(f'  Platform: {found_platform[0]}')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f dist/MarketingFolderCreation/MarketingFolderCreation.exe` exits 0
    - `test -f "dist/MarketingFolderCreation/templates/A250.docx"` exits 0
    - `find dist/MarketingFolderCreation/ -name "qwindows*.dll"` returns at least one result
    - PyInstaller build output does not contain "Error" or "Traceback" (warnings are acceptable)
  </acceptance_criteria>

  <done>dist/MarketingFolderCreation/ folder exists containing MarketingFolderCreation.exe, templates/A250.docx, and qwindows*.dll platform plugin. Build completed without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — launch bundle and test A250 generation</name>
  <files>dist/MarketingFolderCreation/</files>
  <action>Execute human verification: launch exe and test per how-to-verify section</action>
  <verify>Human confirms all checks pass: exe launches in under 3s, A250 generates a docx, no console window, app is interactive</verify>
  <done>Human has approved the build via resume-signal: "approved"</done>

  <what-built>
PyInstaller --onedir bundle at dist/MarketingFolderCreation/MarketingFolderCreation.exe containing:
- PyQt6 application (FolderSetupApp main window)
- templates/A250.docx bundled alongside the exe
- win32com.shell, win32timezone hidden imports for shortcut creation
- No Python installation required to run

The exe should open the project folder setup window without any console terminal.
  </what-built>

  <how-to-verify>
Run these checks in order:

**Check 1 — Exe launches and window appears within 3 seconds:**
1. Open File Explorer, navigate to: dist\MarketingFolderCreation\
2. Double-click MarketingFolderCreation.exe
3. A window titled "Project Folder Setup Tool" should appear within 3 seconds
4. No console/terminal window should appear alongside it
5. The window should show: project name field, 4 path fields with Browse buttons, Run/Cancel/Create A250/Clear Log buttons, progress bar, log panel

**Check 2 — A250 document generation works from bundle:**
1. In the open app window, click "Create A250"
2. Fill in just "Project Title" with any test value (e.g., "Test Project")
3. Fill "Save Location" with your Desktop path (e.g., C:\Users\YourName\Desktop)
4. Click "Generate A250"
5. Windows Explorer should open showing the generated A250_Test Project.docx file on your Desktop
6. Check the log panel shows "[OK] A250 generated: ..."

**Check 3 — No import errors:**
1. The log panel should be empty at startup (no error messages)
2. If you see "[ERR]" messages at startup, note the exact text

**Check 4 — Startup speed (PERF-01):**
1. Close the window
2. Double-click the exe again with a stopwatch/timer
3. Confirm window appears in under 3 seconds
  </how-to-verify>

  <resume-signal>
If all checks pass, type: approved
If any check fails, describe the exact failure (e.g., "window doesn't appear", "A250 error: template not found", "console window appears"). Include any error message text from the log panel or any Windows error dialog.
  </resume-signal>
</task>

</tasks>

<verification>
After Task 1 (automated) and Task 2 (human) complete:

```bash
# Confirm bundle structure is complete
python -c "
from pathlib import Path

bundle = Path('dist/MarketingFolderCreation')
checks = [
    (bundle / 'MarketingFolderCreation.exe').exists(),
    (bundle / 'templates/A250.docx').exists(),
    bool(list(bundle.rglob('qwindows*.dll'))),
]
labels = ['exe', 'template', 'qwindows.dll']
for ok, label in zip(checks, labels):
    print(('[PASS]' if ok else '[FAIL]'), label)
all_pass = all(checks)
print()
print('BUNDLE READY' if all_pass else 'BUNDLE INCOMPLETE — fix failures above')
"
```

Human verification confirms:
- Window appears within 3 seconds (PERF-01)
- A250 document generated successfully from bundled template (PKG-02)
- No console terminal window (PKG-01: console=False)
</verification>

<success_criteria>
1. dist/MarketingFolderCreation/MarketingFolderCreation.exe exists and launches
2. dist/MarketingFolderCreation/templates/A250.docx present in bundle
3. qwindows*.dll platform plugin present in bundle (PyQt6 starts without "No Qt platform plugin" error)
4. Human confirms window appears within 3 seconds (PERF-01)
5. Human confirms A250 generation produces a .docx file (PKG-02: template bundled and resolved at runtime)
6. No console terminal window appears when launching exe
</success_criteria>

<output>
After completion, create `.planning/phases/03-packaging-deployment/03-02-SUMMARY.md`
</output>
