---
phase: quick
plan: 260331-v5u
type: execute
wave: 1
depends_on: []
files_modified: [app.py]
autonomous: true
requirements: [A250-UX-01]

must_haves:
  truths:
    - "A250 dialog shows fields grouped under labeled section headers (Project Info, Client Contact, Billing, Scope & Fee, Output)"
    - "All 23 template fields are present and collected into a flat dict passed to _generate_a250 — docxtpl rendering still works"
    - "Existing tests in tests/test_a250_generation.py continue to pass without modification"
  artifacts:
    - path: "app.py"
      provides: "Refactored _open_a250_form with grouped QGroupBox sections"
      contains: "QGroupBox"
  key_links:
    - from: "app.py _open_a250_form"
      to: "app.py _generate_a250"
      via: "a250_vars dict of {field_name: QLineEdit}"
      pattern: "a250_vars\\[.*\\] = line_edit"
---

<objective>
Refactor the A250 input dialog so fields are grouped into labeled sections, reducing visual overwhelm.

Purpose: The current form is a flat list of 23 raw field-name labels. Grouping into 5 sections with human-readable labels makes the form faster and less error-prone to fill in.
Output: Updated `_open_a250_form` in app.py. No other files change. docxtpl rendering contract unchanged.
</objective>

<execution_context>
@C:/Users/abindal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abindal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Key constraint from existing tests (tests/test_a250_generation.py):
- `_generate_a250(a250_vars)` receives a flat dict `{field_name: QLineEdit}` and calls `.text()` on each value
- The test's `_make_a250_vars` constructs mocks for exactly these 23 keys:
  project_title, project_address, nya_project_code, client_project_code,
  client, client_address, client_phone, client_mobile, client_email, client_invoice, client_office,
  Fname_R, Lname_R, title_R, licenses,
  invoice_to, request_date, work_type, project_description, detailed_scope, fee,
  save_location, a250_creator
- Do NOT rename, add, or remove any of these keys. The docxtpl template variables must stay identical.

Current _open_a250_form (lines 196–237 of app.py): flat QFormLayout with raw field names as labels.
Current _generate_a250 (lines 239–253): reads a250_vars dict, adds current_date, renders template.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor _open_a250_form with grouped QGroupBox sections</name>
  <files>app.py</files>
  <action>
Replace the body of `_open_a250_form` (the portion that builds the form layout and field list) with a grouped layout. Keep `_generate_a250` unchanged.

Add `QGroupBox` to the PyQt6.QtWidgets import line (it is not currently imported).

Build the dialog as follows:

1. Dialog setup and outer layout: same as current (QDialog, resize(700, 780), QVBoxLayout, QScrollArea, container QWidget, QVBoxLayout on container).

2. Define `a250_vars = {}` before the sections.

3. Define groups as an ordered list of (section_title, [(field_key, display_label), ...]) tuples:

   Section "Project Info":
     - ("project_title",       "Project Title")
     - ("project_address",     "Project Address")
     - ("nya_project_code",    "NYA Project Code")
     - ("client_project_code", "Client Project Code")

   Section "Client Contact":
     - ("client",         "Client Name")
     - ("client_address", "Client Address")
     - ("client_phone",   "Phone")
     - ("client_mobile",  "Mobile")
     - ("client_email",   "Email")
     - ("client_office",  "Office")

   Section "Billing":
     - ("client_invoice", "Invoice Address")
     - ("invoice_to",     "Invoice To")
     - ("Fname_R",        "Recipient First Name")
     - ("Lname_R",        "Recipient Last Name")
     - ("title_R",        "Recipient Title")
     - ("licenses",       "Licenses")

   Section "Scope & Fee":
     - ("request_date",       "Request Date")
     - ("work_type",          "Work Type")
     - ("project_description","Project Description")
     - ("detailed_scope",     "Detailed Scope")
     - ("fee",                "Fee")

   Section "Output":
     - ("save_location", "Save Location")
     - ("a250_creator",  "Created By")

4. For each section, create a QGroupBox with the section title. Give it a QFormLayout. For each (key, label) pair: create a QLineEdit, call `form.addRow(label, line_edit)`, and store `a250_vars[key] = line_edit`. Add the QGroupBox to the container's VBoxLayout.

5. Add a small stretch at the end of the container layout so sections don't expand to fill space.

6. Keep the QDialogButtonBox block (Generate A250 / Close) and dialog.exec() exactly as they are now.

Do NOT change `_generate_a250` at all.
Do NOT rename any keys in `a250_vars`.
  </action>
  <verify>
    <automated>cd C:\Users\abindal\Documents\00_Python\MarketingFolderCreation && python -m pytest tests/test_a250_generation.py -v 2>&1</automated>
  </verify>
  <done>
All 5 existing tests in test_a250_generation.py pass.
`_open_a250_form` uses QGroupBox to present fields in 5 labeled sections.
All 23 field keys still populate a250_vars.
  </done>
</task>

</tasks>

<verification>
Run: `python -m pytest tests/test_a250_generation.py -v`
All 5 tests must pass (render_called_with_all_fields, output_filename, save_location_used, save_location_blank_fallback, missing_template_error).

Visual check (optional): `python app.py` → click "Create A250" → confirm 5 labeled group boxes visible in scrollable dialog.
</verification>

<success_criteria>
- All 5 existing A250 tests pass with no modifications to test file
- Dialog shows QGroupBox sections: "Project Info", "Client Contact", "Billing", "Scope & Fee", "Output"
- All 23 docxtpl template variables still render correctly (field keys unchanged)
</success_criteria>

<output>
After completion, create `.planning/quick/260331-v5u-improve-the-a250-input-form-with-grouped/260331-v5u-SUMMARY.md`
</output>
